var name = "Joe"
if (name === "Susan") {
    console.log("The name is correct")
} else {
    console.log("Expected the name to be Susan")
} //if (console.log.innerTEXT === "Expected the name to be Susan") {var name = "Susan"}

var height = "74"
var heightSelector = document.querySelector('#height')
	heightSelector.innerHTML = "74"
		heightSelector.innerHTML = parseInt(friendSelector.innerHTML) - 10
// Don't worry about this
module.exports = { name, height
}
